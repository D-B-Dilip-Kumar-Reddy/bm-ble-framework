[pytest]
pythonpath = src
asyncio_mode = auto
testpaths = tests
python_files  = test_*.py
python_classes= Test*
python_functions = test_*
markers =
    unit: fast unit tests with no hardware
    integration: mocked BLE tests with no hardware
    functional: real camera hardware tests
